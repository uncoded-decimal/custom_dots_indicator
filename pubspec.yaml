name: custom_dots_indicator
description: "A highly customisable dots page Indicator. Allows for customisation of dots and label."
version: 1.0.1
homepage: https://github.com/uncoded-decimal/custom_dots_indicator

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

topics:
  - pagination
  - indicator
